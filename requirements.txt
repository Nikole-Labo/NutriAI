# Chunking only needs text splitters + core; do NOT install full `langchain` 0.3.x
# here — it pins langchain-core 0.3.x and breaks langchain-classic / langgraph, etc.
langchain-core>=1.2.17,<2.0.0
langchain-text-splitters>=1.1.0,<2.0.0

pydantic>=2.9.2,<3
python-dotenv>=1.0.1
tqdm>=4.66.5
requests>=2.32.0
datasets>=3.1.0
tiktoken>=0.8.0
pytest>=8.3.0

# Stage 2 — Qdrant + hybrid (dense MiniLM + sparse BM25 via FastEmbed)
qdrant-client>=1.12.0,<2.0.0
sentence-transformers>=3.0.0,<4.0.0
fastembed>=0.4.0,<0.8.0
